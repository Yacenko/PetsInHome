import React from 'react';

/**
 * Component to show texts in an application
 * @param props
 * @returns {XML}
 * @constructor
 */
const Text = (props) => {
	return (
		<div>{props.text}</div>
	);
};

export default Text;
